const styles = ({spacing}) => ({
	root: {
		display: 'flex',
		flexDirection: 'column',
		minHeight: '100vh',
	},
	main: {
		marginTop: spacing(2),
		marginBottom: spacing(2),
	},
});

export default styles;
